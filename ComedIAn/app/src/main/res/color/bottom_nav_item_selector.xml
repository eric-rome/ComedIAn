<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Estado cuando el elemento no está seleccionado -->
    <item android:state_checked="false" android:color="@android:color/transparent" />
    <!-- Estado cuando el elemento está seleccionado -->
    <item android:state_checked="true" android:color="@color/bottom_nav_item_pressed_color" />
</selector>